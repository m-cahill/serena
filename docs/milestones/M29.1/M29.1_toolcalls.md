# M29.1 — Tool call log

**Milestone:** Gradio 6 event / JS compatibility  
**Started:** 2026-03-25 (UTC)

## Discovery — `_js=` in `modules/` (required first step)

Command equivalent: `rg "_js\\s*=" modules/ --glob "*.py"`

Files with `_js=` keyword usage (Gradio event kwargs, pre–Gradio 6):

| File | Notes |
|------|--------|
| `modules/ui.py` | submit / training hooks |
| `modules/ui_txt2img_tab.py` | submit, switch resolution, etc. |
| `modules/ui_img2img_tab.py` | submit, tab index, change handlers |
| `modules/ui_prompt_styles.py` | confirm, then() chain |
| `modules/ui_settings.py` | reload, localization, checkpoint |
| `modules/ui_toprow.py` | interrupt click, clear prompt |
| `modules/ui_extra_networks_user_metadata.py` | closePopup, refresh card |
| `modules/ui_postprocessing.py` | submit_extras |
| `modules/ui_extra_networks.py` | select, load, then chains |
| `modules/ui_extensions.py` | extensions_apply, config restore |
| `modules/ui_common.py` | gallery hooks, popup, closePopup |
| `modules/ui_checkpoint_merger.py` | modelmerger |
| `modules/ui_components.py` | InputAccordion change |
| `modules/processing_scripts/seed.py` | random seed buttons |
| `modules/infotext_utils.py` | infotext bindings |

**Not modified:** `modules/ui_gradio_extensions.py` line `script_js = ...` — variable name, not `_js=` kwarg.

**Fix:** Gradio 6 uses **`js=`** instead of **`_js=`** on `.click()`, `.change()`, `.select()`, `.load()`, `.then()`, etc.

| Timestamp (UTC) | Tool | Purpose | Target |
|-----------------|------|---------|--------|
| 2026-03-25 | write | M29.1_toolcalls.md discovery | docs/milestones/M29.1/ |
| 2026-03-25 | shell | Replace `_js=` → `js=` in modules | 15 files: `infotext_utils.py`, `processing_scripts/seed.py`, `ui.py`, `ui_checkpoint_merger.py`, `ui_common.py`, `ui_components.py`, `ui_extensions.py`, `ui_extra_networks.py`, `ui_extra_networks_user_metadata.py`, `ui_img2img_tab.py`, `ui_postprocessing.py`, `ui_prompt_styles.py`, `ui_settings.py`, `ui_toprow.py`, `ui_txt2img_tab.py` |

## Binding CI recovery (`m29.1-binding-ci-recovery`)

| Timestamp (UTC) | Action | Notes |
|-----------------|--------|--------|
| 2026-03-25 | Diagnosis-first doc | `M29_run1.md` — table of root causes (sampler_index, runtime_metrics, cmd-flags, progress, img2img mask null, txt2img contract mocks) before code commits |
| 2026-03-25 | Code | API: drop `sampler_index=None`; `get_cmd_flags` filter to `FlagsModel`; `ProgressResponse.current_task`; img2img `mask` → `Optional[str]`; `process_images` no `request.runtime_metrics`; txt2img contract test mocks `apply_token_merging` |
| 2026-03-26 | Doc | `M29_run1.md` Round 2 — failure cluster + schema hypotheses (`SamplerItem.options`, `ProgressResponse` nullables, `cmd-flags` encode) before code |
| 2026-03-26 | Code | `SamplerItem.options` → `dict[str, Any]`; `ProgressResponse` `current_image`/`textinfo` → `Optional[str]`; `get_cmd_flags` `jsonable_encoder` + `model_validate` |
| 2026-03-26 | Doc | `M29_run1.md` Round 3 — numbered failure list from run `23573723272` + fix order (`inpainting_mask_invert` bool vs int, seam tests patch `execute`) before code |
| 2026-03-26 | Code | img2img API: `inpainting_mask_invert` → `Union[int, bool]`; seam tests: mock `ProcessingRunner.execute` instead of `process_images_inner` |
| 2026-03-26 | shell | `gh pr create` → **PR #79** (`m29.1-binding-ci-recovery` → `main`); initial checks: eslint pass, ruff fail, smoke fail |
| 2026-03-26 | Code | `4611740b` — Ruff/CI hygiene for PR green: seam test import, snapshot imports, `processing.py` F811 ignore, exclude `ui_components.pyi` |
| 2026-03-26 | write | `M29_run1.md` — PR #79 URL + first-check table + follow-up commit note |
| 2026-03-26 | shell | `gh pr checks 79` — post-`4611740b`: eslint pass, ruff pass, smoke fail (`wait-for-it` / port 7860); run `23576902847` |
| 2026-03-26 | write | `M29_run1.md` — second snapshot table + smoke failure note (`output.txt` artifact) |

### Round 4 — smoke startup / bind (`23576902847`)

| Timestamp (UTC) | Action | Notes |
|-----------------|--------|-------|
| 2026-03-26 | Diagnosis | Smoke **`23576902847`**: **`wait-for-it`** on **`127.0.0.1:7860`** — failure mode = **server never binds** (process dies in **`create_ui()`**), not pytest HTTP failures from **`23573723272`**. |
| 2026-03-26 | Artifact | **`output/output.txt`**: (1) thread **`set_model_type`** — **`AttributeError`**: **`LatentDiffusion`** has no **`cond_stage_model`** (`sd_models.py` ~397 **`hasattr(model.cond_stage_model, 'model')`**). (2) Main thread — **`TypeError`**: **`Image.__init__() got an unexpected keyword argument 'source'`** from **`ui_img2img_tab.py`** ~**69** (`source=` / legacy sketch kwargs vs Gradio **6.10**). |
| 2026-03-26 | Code | **`sd_models.set_model_type`**: test **`cond_stage_model`** via **`getattr`** before **`.model`**. **`gr.Image`**: **`source=` → `sources=`** in **`ui_img2img_tab`**, **`ui_postprocessing`**, **`ui`** (pnginfo); remove **`tool` / `brush_color`** from img2img **`Image`** constructors (upload-only; aligns with Gradio 6 **`Image`** signature). |
| 2026-03-26 | write | **`M29_run1.md`** — Round 4 section (run ID, mode shift, tracebacks, classification, fix direction). |
| 2026-03-26 | shell | **`gh pr checks 79`** — post Image/sd_models commit: eslint **pass**, ruff **pass**, smoke **fail** (`23578226657`). |
| 2026-03-26 | Artifact | **`23578226657` `output.txt`**: first fatal **`EventListenerMethod` / `js`** on **`ui_toprow` interrupt `.click`** (not `gr.Image` / `source` in this run). |
| 2026-03-26 | Code | **`ui_toprow`**: drop **`js=`** on interrupt + clear-prompt **`.click`** (Gradio 6.10 CI path). |
| 2026-03-26 | write | **`M29_run1.md`** — Round 4 post-push note + Round 4b direction. |
| 2026-03-26 | Code | Dropped **`gradio_event_compat`** ( **`EventListener.__init__`** conflict with **`Component`** cooperative init on CI). **`gradio_extensons`**: wrap **`gradio.blocks.BlocksConfig.set_event_trigger`** to strip **`js` / `_js`** at dependency registration. |
