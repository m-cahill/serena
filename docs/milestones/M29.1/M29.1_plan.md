# M29.1 — Gradio 6 event / JS compatibility

## Objective

Restore **green Quality CI** by aligning legacy WebUI event wiring with **Gradio 6** (`js=` replaces `_js=` on dependency triggers).

## Constraints

- Minimal, localized edits; no UI redesign.
- No dependency changes in M29.1 (lockfile already on Gradio 6.x).
- No extension API / JSON API changes.
- No changes to M29 performance instrumentation logic.

## Definition of done

- [ ] All Gradio event calls use `js=` (not `_js=`).
- [ ] Quality CI green; coverage ≥42%; `performance_snapshot.txt` produced.
- [ ] Update `docs/milestones/M29/M29_run1.md` with binding run + artifact note.
