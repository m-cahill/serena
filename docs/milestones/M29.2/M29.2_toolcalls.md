# M29.2 — Post-merge Quality recovery

**Goal:** Restore binding **Quality** green on `main` after merge commit **`603b1dc026ee96e5e00c6aa0eda02f1306d12476`** (PR **#79**).

**Failing reference run:** https://github.com/m-cahill/serena/actions/runs/23615984557

---

## Diagnosis (run `23615984557`)

### A. Failing tests (pytest short summary)

| # | Test | Assertion / error |
|---|------|-------------------|
| 1 | `test/smoke/test_utils.py::test_get_api_url[sdapi/v1/cmd-flags]` | `AssertionError: assert 500 == 200` |
| 2 | `test/quality/test_api_extended.py::test_get_api_endpoint[sdapi/v1/cmd-flags]` | `AssertionError: assert 500 == 200` |
| 3 | `test/quality/test_txt2img_runner_contract.py::test_txt2img_path_uses_runner` | `AssertionError: assert 'runner_execute' in []` |

### B. Traceback snippets (Quality log / `output.txt`)

**cmd-flags (HTTP 500 → Pydantic):**

```text
FAILED test/smoke/test_utils.py::test_get_api_url[sdapi/v1/cmd-flags] - AssertionError: assert 500 == 200
FAILED test/quality/test_api_extended.py::test_get_api_endpoint[sdapi/v1/cmd-flags] - AssertionError: assert 500 == 200
```

Server log (`output` artifact):

```text
*** API error: GET: http://127.0.0.1:7860/sdapi/v1/cmd-flags {'error': 'ValidationError', ...
pydantic_core._pydantic_core.ValidationError: 27 validation errors for Flags
loglevel
  Input should be a valid string [type=string_type, input_value=None, input_type=NoneType]
models_dir
  Input should be a valid string ...
```

**Runner contract test:**

```text
FAILED test/quality/test_txt2img_runner_contract.py::test_txt2img_path_uses_runner - AssertionError: assert 'runner_execute' in []
```

### C. Classification

| Bucket | Verdict |
|--------|---------|
| **cmd-flags / `get_cmd_flags`** | **Continuation** of M29.1 Round 7: dual-stack `get_cmd_flags` uses **`model_validate`** on Pydantic v2; explicit **`None`** for **`str`-typed** `FlagsModel` fields fails validation. |
| **`test_txt2img_path_uses_runner`** | **Test harness regression (new):** the test replaces **`ProcessingRunner`** with **`TestRunner`**, then assigns **`ProcessingRunner.execute = fake_execute`**. After the first patch, **`ProcessingRunner`** is **`TestRunner`**, so the second patch **overwrites `TestRunner.execute`**, dropping the line that appended **`"runner_execute"`**. |

### D. Root cause hypothesis

- **`vars(shared.cmd_opts)`** includes keys whose value is **`None`** for optional CLI strings (e.g. **`loglevel`**, **`models_dir`**, **`ckpt_dir`**, **`vae_dir`**, **`gfpgan_model`**, …).
- **`FlagsModel`** builds fields as **`str`** when **`argparse` default is `None`** (`modules/api/models.py` loop uses `_type = str` unless default is non-`None`).
- **Pydantic v2 `model_validate`** treats explicit **`None`** as input and rejects it for **`str`** fields.
- **Smoke** uses **Pydantic v1** + **`parse_obj`**, which tolerates this combination more leniently in practice.
- **Quality** uses **Pydantic v2** → stricter validation → **500** on `/sdapi/v1/cmd-flags`.

---

## Invariants (must not change)

- JSON/API **response shape** for cmd-flags (same fields as before; omission of `None` keys still validates to model defaults).
- **CLI / `cmd_opts` semantics** unchanged.
- **Runner seam** behavior in production unchanged; test fix only restores the contract assertion.

## Allowed change surface

- **`modules/api/api.py`** — `get_cmd_flags` (filter **`None`** before validation).
- **`modules/api/models.py`** — only if filtering is insufficient (prefer not).
- **`test/quality/test_txt2img_runner_contract.py`** — minimal test correction if required.

## Fix order (implemented)

1. **`get_cmd_flags`:** build `out` excluding **`v is None`** (keys omitted → defaults apply on validate).
2. **Runner test:** **`fake_execute`** must record **`runner_execute`** (remove conflicting **`TestRunner` + double patch** pattern).

---

## Validation (filled after CI)

| Field | Value |
|-------|--------|
| Branch | `m29.2-quality-recovery` |
| PR | (pending) |
| Quality run ID | (pending) |
| Result | (pending) |
| Coverage % | (pending) |
| `performance_snapshot.txt` | (pending) |
