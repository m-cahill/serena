# Input spec for `requirements-ci.txt` (M26). Regeneration: see
# docs/architecture/ci_environment_contract.md (uv pip compile command).
#
# Mirrors post-M25 Quality CI intent. The historical open_clip GitHub ZIP is
# omitted from compilation inputs; see contract doc (PyPI open-clip-torch pin).

--extra-index-url https://download.pytorch.org/whl/cpu

# M28b batch 1: HTTP stack — versions bumped via `uv pip compile --upgrade-package …` (see requirements-ci.txt header).
# M28b batch 2: Pillow 10.x path; batch 5a moves to **12.x** (CVE-2026-25990). **Co-upgrades required by PyPI:** `blendmodes` 2024→2025 (Pillow<11→open), **numpy** 1.x→2.x, **gradio** 3.x→**6.5.x** (first line with `pillow<13`).
# M28b batch 3: API stack — `fastapi` / `starlette` / `h11` (see lock header `--upgrade-package` list).
# M28b batch 4: tooling / infra — setuptools, wheel, filelock, GitPython (see lock header).
wait-for-it==2.3.0
pytest==7.4.4
pytest-base-url==2.1.0
pytest-cov==4.1.0
setuptools>=70,<80
wheel>=0.43,<1
torch==2.1.2
torchvision==0.16.2
# OpenAI CLIP: not in this lockfile — PEP 517 metadata for the GitHub archive fails on CI.
# Quality workflow downloads the pinned zip, unzips, and `pip install --no-build-isolation` the tree.
# open_clip GitHub zip omitted: duplicate ZIP entries break uv; PyPI pin below matches post-install state.
GitPython>=3.1.40,<4
pillow>=12.0.0,<13
accelerate==0.21.0
blendmodes==2025
clean-fid==0.1.35
filelock>=3.13,<4
diskcache==5.6.3
einops==0.4.1
facexlib==0.3.0
fastapi>=0.110,<1
starlette>=0.37,<1
h11>=0.14,<1
gradio==6.5.0
# httpcore 0.15 pins h11<0.13 — incompatible with M28b h11>=0.14; bump client stack with FastAPI batch.
httpcore>=1.0,<2
inflection==0.5.1
jsonmerge==1.8.0
kornia==0.6.7
lark==1.1.2
numpy>=2.0.2,<3
omegaconf==2.2.3
open-clip-torch==2.20.0
piexif==1.1.3
protobuf==3.20.2
psutil==5.9.5
pytorch_lightning==1.9.4
resize-right==0.0.2
safetensors==0.4.2
scikit-image==0.21.0
spandrel==0.3.4
spandrel-extra-arches==0.1.1
tomesd==0.1.3
torchdiffeq==0.2.3
torchsde==0.2.6
transformers==4.30.2
httpx>=0.27,<1
pillow-avif-plugin==1.4.3
